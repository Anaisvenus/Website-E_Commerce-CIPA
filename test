
  <!-- shop section -->
  <!-- <a href="backend/product_detail.php?produit_id=<?php echo $product['produit_id']; ?>">View Product</a> -->

  <!-- <section class=""> -->
    <!-- <div class="container mx-auto p-4">
      <h1 class="text-3xl font-bold text-center mb-6">Liste des Produits</h1>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6" produit_id="produits-container">
      </div>
    </div> -->

    <!-- <script>
      fetch('backend/index.php')
      .then(response => response.json())
      .then(data => {
          const productsContainer = document.getElementById('produits-container');

          if (data.length === 0) {
              productsContainer.innerHTML = `<p class="col-span-full text-center text-red-500 font-semibold">
                  Aucun produit trouvé
              </p>`;
          } else { -->
              <!-- data.forEach(produits => {
                  const productHTML = `
                  <div class="bg-white shadow-md rounded-lg overflow-hidden">
                    <div class="product-image">
                      <img src="images/background removed images/IMG_7319.PNG" alt="Oxivir Excel Foam">
                    </div>
                      <div class="p-4">
                          <h1 class="font-bold text-lg mb-2">${produits.nom}</h1>
                          <p class="text-sm text-gray-500">${produits.description}</p>
                          <p class="font-semibold text-green-600 mt-2">${produits.prix_unitaire} FCFA</p> -->
                          
                          <!-- Product Link -->
                          <!-- <a href="backend/product_detail.php?produit_id=${produits.produit_id}" class="bg-blue-500 text-white text-sm px-4 py-2 rounded-md hover:bg-blue-600 mt-4 block text-center">
                              Voir les détails
                          </a> -->
                          <!-- <button class="bg-blue-500 text-white text-sm w-fu px-4 py-2 rounded-md hover:bg-blue-600 mt-4 block text-center"
                            onclick="addToCart('${produits.nom}', ${produits.prix_unitaire}, 'quantity-${produits.produit_id}')">
                            Acheter immédiatement
                          </button> -->

                          <!-- Quantity Selector and Add to Cart Button -->
                          <!-- <div class="mt-4 flex items-center justify-between">
                              <input class="w-16 p-2 border rounded-lg text-sm" 
                                    produit_id="quantity-${produits.produit_id}" 
                                    type="number" 
                                    value="1" 
                                    min="1">
                              <button class="add-to-cart-btn bg-blue-500 text-white text-sm px-4 py-2 rounded-md hover:bg-blue-600"
                                      onclick="addToCart('${produits.nom}', ${produits.prix_unitaire}, 'quantity-${produits.produit_id}')">
                                  Ajouter au panier
                              </button>
                          </div>
                      </div>
                  </div>`;
                  
                  productsContainer.innerHTML += productHTML;
              });
          }
      })
      .catch(error => {
          console.error('Error fetching data:', error);
          document.getElementById('produits-container').innerHTML = `<p class="col-span-full text-center text-red-500 font-semibold">
              Erreur lors de la récupération des données
          </p>`;
      }); -->

    <!-- </script> -->
  <!-- </section> -->
